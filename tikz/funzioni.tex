\section{Funzioni}

\begin{center}
	\begin{tikzpicture}
		\begin{axis}[
				title={Funzioni},
				font=\normalsize,
				width=13cm,
				axis lines = center,
				xlabel = $x$,
				ylabel = $y$,
				xtick = {-4, -2, 2, 4},
				ytick = {-4, -2, 2, 4, 6, 8},
				ymin = -5, ymax = 8,
				xmin = -5, xmax = 5,
				grid = both,
				grid style = dashed,
				legend pos = north west,
				enlargelimits
			]

			\addplot [
				name path=f,
				domain=0:5,
				samples=100,
				thick,
				color=red
			]
			{log2(x)};

			\addplot [
				name path=g,
				domain=0:5,
				samples=100,
				thick,
				color=blue
			]
			{sqrt(x)};

			\addplot [
				domain=-5:5,
				samples=100,
				thick,
				color=yellow,
			]
			{2^x};

			\addplot [
				domain=-5:5,
				samples=100,
				thick,
				color=green
			]
			{8 * exp(-x^2)};

			\legend{
				$\log_2{x}$,
				$\sqrt{x}$,
				$2^x$,
				$8 e^{-x^2}$
			}
		\end{axis}
	\end{tikzpicture}
\end{center}

\begin{center}
	\begin{tikzpicture}
		\begin{axis}[
				title={Data},
				font=\normalsize,
				width=12cm,
				axis lines = center,
				xlabel = $x$,
				ylabel = $y$,
				xtick = {-8, -4, 4, 8},
				ytick = {-8, -4, 4, 8},
				ymin = -10, ymax = 10,
				xmin = -10, xmax = 10,
				grid = both,
				grid style = dotted,
				legend pos = north west,
				enlargelimits
			]

			\addplot [
				domain=-10:10,
				samples=100,
				thick,
				color=violet,
				mark=*
			]
			coordinates { (-8, -8) (-4, -4) (0, 0) (4, 4) (8, 8) };
			\addlegendentry{$x$};
		\end{axis}
	\end{tikzpicture}
\end{center}

\begin{center}
	\begin{tikzpicture}
		\begin{axis}[
				axis lines = center,
				axis line style = thick,
				xtick={-2, -1, 1, 2},
				xticklabels={-2, -1, 1, 2},
				ytick={-2, -1, 1, 2},
				yticklabels={-2, -1, 1, 2},
				samples=100,
				xmin=-2, xmax=2, ymin=-2, ymax=2
			]

			\node[anchor=north east] at (0, 0) {$O$};

			\addplot[name path=f, domain={-2:2}, thick, blue] {x^3 + x^2 - x - 0.5};

			\addplot[name path=x, ultra thin] { 0 };

			\addplot[fill=blue, fill opacity=0.2] fill between [of=f and x, soft clip={domain=-1.45:0.85}];
		\end{axis}
	\end{tikzpicture}
\end{center}